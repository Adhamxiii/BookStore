{
  "name": "bookstore-project",
  "version": "1.0.0",
  "description": "BookStore Full Stack Application",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
